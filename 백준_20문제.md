# 백준 Silver~Gold 초반 20문제

> 야놀자 백엔드 개발자 코딩테스트 대비
> 목표: 각 유형별 핵심 문제 1~2개씩 빠르게 감 잡기

---

## 풀이 진행 현황

| # | 문제번호 | 문제명 | 난이도 | 유형 | 완료 |
|---|---------|--------|--------|------|------|
| 1 | 1926 | 그림 | Silver 1 | BFS | ☐ |
| 2 | 2178 | 미로 탐색 | Silver 1 | BFS | ☐ |
| 3 | 1260 | DFS와 BFS | Silver 2 | DFS/BFS | ☐ |
| 4 | 2606 | 바이러스 | Silver 3 | BFS/DFS | ☐ |
| 5 | 1463 | 1로 만들기 | Silver 3 | DP | ☐ |
| 6 | 1149 | RGB거리 | Silver 1 | DP | ☐ |
| 7 | 2579 | 계단 오르기 | Silver 3 | DP | ☐ |
| 8 | 9461 | 파도반 수열 | Silver 3 | DP | ☐ |
| 9 | 1931 | 회의실 배정 | Silver 1 | 그리디 | ☐ |
| 10 | 11399 | ATM | Silver 4 | 그리디/정렬 | ☐ |
| 11 | 1874 | 스택 수열 | Silver 2 | 스택 | ☐ |
| 12 | 2504 | 괄호의 값 | Gold 5 | 스택 | ☐ |
| 13 | 1753 | 최단경로 | Gold 4 | 다익스트라 | ☐ |
| 14 | 1916 | 최소비용 구하기 | Gold 5 | 다익스트라 | ☐ |
| 15 | 14502 | 연구소 | Gold 4 | BFS + 브루트포스 | ☐ |
| 16 | 7576 | 토마토 | Gold 5 | BFS | ☐ |
| 17 | 11053 | 가장 긴 증가하는 부분 수열 | Silver 2 | DP | ☐ |
| 18 | 1197 | 최소 스패닝 트리 | Gold 4 | MST(크루스칼) | ☐ |
| 19 | 2252 | 줄 세우기 | Gold 3 | 위상정렬 | ☐ |
| 20 | 1927 | 최소 힙 | Silver 2 | 힙/우선순위 큐 | ☐ |

---

## 유형별 핵심 정리

### BFS (너비 우선 탐색)
> 최단 거리, 레벨 탐색에 사용

```java
Queue<int[]> queue = new LinkedList<>();
queue.offer(new int[]{startX, startY});
visited[startX][startY] = true;

while (!queue.isEmpty()) {
    int[] cur = queue.poll();
    for (int[] dir : dirs) {
        int nx = cur[0] + dir[0];
        int ny = cur[1] + dir[1];
        if (isValid(nx, ny) && !visited[nx][ny]) {
            visited[nx][ny] = true;
            queue.offer(new int[]{nx, ny});
        }
    }
}
```

**추천 풀이 순서**: 2606 → 1926 → 2178 → 7576 → 14502

---

### DFS (깊이 우선 탐색)
> 경우의 수, 연결 요소 탐색에 사용

```java
void dfs(int node) {
    visited[node] = true;
    for (int next : graph.get(node)) {
        if (!visited[next]) {
            dfs(next);
        }
    }
}
```

**추천 풀이 순서**: 1260 → 2606

---

### DP (동적 프로그래밍)
> 점화식 세우는 게 핵심

```java
// 1463 - 1로 만들기
int[] dp = new int[n + 1];
for (int i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + 1;
    if (i % 2 == 0) dp[i] = Math.min(dp[i], dp[i / 2] + 1);
    if (i % 3 == 0) dp[i] = Math.min(dp[i], dp[i / 3] + 1);
}
```

**추천 풀이 순서**: 1463 → 1149 → 2579 → 11053 → 9461

---

### 그리디
> 매 순간 최선의 선택. 정렬과 함께 자주 출제

```java
// 1931 - 회의실 배정 핵심 아이디어
// 종료 시간 기준 정렬 → 종료 시간 빠른 것부터 선택
Arrays.sort(meetings, (a, b) -> a[1] != b[1] ? a[1] - b[1] : a[0] - b[0]);
```

**추천 풀이 순서**: 11399 → 1931

---

### 스택
> 괄호, 수열, 후위 표기법에 자주 사용

```java
// 1874 - 스택 수열 핵심 아이디어
Stack<Integer> stack = new Stack<>();
int num = 1;
for (int target : sequence) {
    while (num <= target) stack.push(num++);  // push
    if (stack.peek() != target) { impossible = true; break; }
    stack.pop();
}
```

**추천 풀이 순서**: 1874 → 2504

---

### 다익스트라
> 가중치 있는 그래프 최단경로

```java
// 기본 구조
PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[1] - b[1]);
pq.offer(new int[]{start, 0});
dist[start] = 0;

while (!pq.isEmpty()) {
    int[] cur = pq.poll();
    int node = cur[0], cost = cur[1];
    if (dist[node] < cost) continue;  // 이미 더 짧은 경로 있으면 skip
    for (int[] next : graph.get(node)) {
        if (dist[next[0]] > cost + next[1]) {
            dist[next[0]] = cost + next[1];
            pq.offer(new int[]{next[0], dist[next[0]]});
        }
    }
}
```

**추천 풀이 순서**: 1916 → 1753

---

### 최소 스패닝 트리 (MST)
> 크루스칼: Union-Find + 간선 정렬

```java
// Union-Find 기본 구조
int[] parent = new int[n + 1];
for (int i = 0; i <= n; i++) parent[i] = i;

int find(int x) {
    if (parent[x] != x) parent[x] = find(parent[x]);  // 경로 압축
    return parent[x];
}

boolean union(int a, int b) {
    a = find(a); b = find(b);
    if (a == b) return false;
    parent[a] = b;
    return true;
}
```

**추천 풀이 순서**: 1197

---

### 위상 정렬
> 순서가 있는 작업, 선수 과목 문제

```java
// 진입 차수 0인 노드부터 큐에 삽입
Queue<Integer> queue = new LinkedList<>();
for (int i = 1; i <= n; i++) {
    if (inDegree[i] == 0) queue.offer(i);
}
while (!queue.isEmpty()) {
    int cur = queue.poll();
    result.add(cur);
    for (int next : graph.get(cur)) {
        if (--inDegree[next] == 0) queue.offer(next);
    }
}
```

**추천 풀이 순서**: 2252

---

## 추천 풀이 순서 (전체)

```
Week 1: BFS/DFS 기초
  2606 → 1260 → 1926 → 2178

Week 2: DP 기초
  1463 → 1149 → 2579 → 11053

Week 3: 그리디 + 스택
  11399 → 1931 → 1927 → 1874 → 2504

Week 4: 그래프 심화
  1916 → 1753 → 7576 → 14502 → 1197 → 2252 → 9461
```

---

## 팁

- 문제 읽고 **유형 파악**이 먼저
- 막히면 30분 이상 고민하지 말고 풀이 참고 후 **직접 다시 구현**
- 풀고 나서 **시간복잡도** 반드시 체크
- 코딩테스트는 **완성된 코드**가 목표, 완벽한 최적화보다 동작하는 코드 먼저
