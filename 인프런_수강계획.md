# 인프런 보유 강의 — 수강 우선순위

> ⚠️ 계정 비밀번호는 이 문서에 기록하지 않음. 별도 비밀번호 관리 도구 사용 권장.
> 기준일 2026-09-23 · 현대오토에버(인증/계정), CJ대한통운(더운반) 지원 완료 상태

---

## 🔴 1순위 — 지금 듣기 (면접 직결)

### ① 모든 개발자를 위한 HTTP 웹 기본 지식 (김영한) `sanharnd1`
**약 7시간 · 주 3시간이면 2~3주**

**왜 1순위인가**: 네 이력서의 핵심이 **"쿠키는 도메인을 넘지 못해서 Redis 세션 중앙화로 풀었다"** 인데, 이 강의가 정확히 그 배경 지식이다. 쿠키의 domain/path 속성, 세션과 토큰의 차이, 인증 헤더, 캐시 동작을 다룬다.

면접에서 *"왜 쿠키로는 안 됐나요?"*, *"세션과 토큰 중 왜 세션이었나요?"* 를 물었을 때 **HTTP 스펙 수준에서 설명할 수 있으면 답변의 무게가 달라진다.**

집중해서 볼 것: 쿠키(domain, path, Secure, SameSite) / HTTP 헤더 / 상태코드 / 캐시

### ② 실전! 스프링 부트와 JPA 활용2 — API 개발과 성능 최적화 (김영한) `sanharnd2`
**약 8시간 · 3주**

**왜 필요한가**: JPA 면접의 90%는 **N+1**에서 시작한다. 이 강의가 N+1을 지연로딩·페치조인·DTO 직접조회·batch size 순으로 단계별로 해결하는 과정을 보여준다. 실무에서 JPA를 쓰고 있어도 **"왜 그 방법을 택했는지"** 를 말하려면 이 강의가 가장 빠르다.

집중해서 볼 것: API 개발 기본 → 지연로딩과 조회 성능 최적화 → 컬렉션 조회 최적화

---

## 🟡 2순위 — 1순위 끝난 뒤

### ③ 스프링 MVC 1편 — 백엔드 웹 개발 핵심 기술 `sanharnd1`
**약 17시간 · 길다. 필요한 부분만 골라 듣기**

olive-market에서 `JwtAuthenticationFilter` 를 `UsernamePasswordAuthenticationFilter` 앞에 끼워 넣었는데, 면접에서 **"왜 필터에 넣었나, 인터셉터는 왜 아닌가"** 를 물을 가능성이 높다. 그 답이 이 강의에 있다.

골라 들을 것: **서블릿 / 필터 / 인터셉터 / 디스패처 서블릿 동작 흐름**
(MVC 기본 구조는 8년차면 이미 아는 내용이므로 건너뛰어도 무방)

### ④ 실전! Querydsl `sanharnd2`
**약 6시간**

부킹엔진에서 QueryDSL을 실무로 썼고 이력서·기술스택에도 명시했으므로, 면접 질문이 들어올 수 있다. 동적 쿼리와 DTO 프로젝션 중심으로.

---

## 🟢 3순위 — 여유 있을 때 / 장기

### ⑤ 스프링 핵심 원리 - 고급 `sanharnd1`
**약 15시간**

프록시 패턴, 데코레이터, 동적 프록시, AOP 원리. 면접 단골이지만 분량이 크다. 면접이 잡히면 이건 후순위로 미룰 것.

### ⑥ 스프링 DB 1편 - 데이터 접근 핵심 원리 `sanharnd1`
**약 12시간**

트랜잭션 전파와 예외 처리 원리. **결제-예약 상태 전이를 설명할 때 트랜잭션 경계를 말할 수 있으면 좋다.** 다만 지금 당장은 아니다.

### ⑦ AWS 입문 / 중·상급 (Simon Kim) `sanharnd5`
실무로 AWS Elastic Beanstalk 배포와 인프라 전환을 해봤으므로 기본은 있다. 면접에서 인프라 질문이 나올 조짐이 보이면 그때.

---

## ⬜ 지금은 듣지 않아도 되는 것

| 강의 | 이유 |
|---|---|
| 스프링 핵심 원리 - 기본 | IoC/DI는 8년차면 이미 체득. 개념 정리는 `gocj/Spring_개념학습.md` 로 충분 |
| 자바 ORM 표준 JPA 프로그래밍 - 기본편 | JPA를 실무로 쓰고 있음. 기본편은 과함 |
| 실전! 스프링 부트와 JPA 활용1 | 기초 웹 개발 단계. 활용2로 바로 가도 됨 |
| 실전! 스프링 데이터 JPA | 실무 사용 중. 필요할 때 검색으로 해결 가능 |
| 스프링 MVC 2편 | 검증·파일업로드·타임리프 중심. 백엔드 면접 비중 낮음 |
| TypeScript / Flutter (`sanharnd3`) | 프론트·모바일. 현재 목표와 무관 |
| 자바 입문 / UX·UI (`sanharnd4`) | 수준이 맞지 않음 |
| IT 회사에서 살아남기 A to Z (`sanharnd4`) | 신입 대상 개괄. 8년차에겐 불필요 |
| AWS 클라우드 보안·해킹 (`sanharnd5`) | 보안 직무 아님 |

---

## 월·수·금 인강 슬롯 배치안 (주 3시간)

| 시기 | 강의 |
|---|---|
| **9월 말 ~ 10월 중순** | ① HTTP 웹 기본 지식 (7h) |
| **10월 중순 ~ 11월 초** | ② JPA 활용2 (8h) |
| **11월** | ③ MVC 1편 (필터·인터셉터만 발췌) + ④ Querydsl |
| **12월 이후** | ⑤ 핵심 원리 고급 / ⑥ DB 1편 |

> ⚡ **면접 일정이 잡히면 모든 강의를 중단하고 면접 대비로 전환할 것.**
> 강의는 언제든 다시 들을 수 있지만 면접은 한 번뿐이다.

---

## 강의보다 먼저 할 것

⚠️ **부킹엔진 코드를 열어 아래 세 가지를 정리하는 일이 어떤 강의보다 우선이다.**

1. 세션 키를 어떻게 공유했는가 (도메인 구조, 세션 ID 전달 방식, Redis 키 설계)
2. 결제-예약 상태 전이 (상태값 종류, 실패 시 보상 처리)
3. MV 전환 (전환 전 에러 형태, 갱신 주기)

강의는 일반 지식을 채우지만, 이 셋은 **너만 가진 답변**이다. 재직 중에만 확인 가능하다.
